export enum CommandEnum {
  Download = 'download',
  Feedback = 'feedback',
  Help = 'help',
  Join = 'join',
  PutPassword = 'putpassword',
  Split = 'split',
  Start = 'start',
  Test = 'test',
  Version = 'version',
}
