export enum CommandEnum {
  Download = 'download',
  Feedback = 'feedback',
  Help = 'help',
  Join = 'join',
  Pro = 'pro',
  PutPassword = 'putpassword',
  Split = 'split',
  Start = 'start',
  Summary = 'summary',
  Test = 'test',
  Version = 'version',
}
